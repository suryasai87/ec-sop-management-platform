# Databricks Dash App

This is a Databricks Dash application connected to the DEFAULT profile.

## Structure
- `app.py` - Main Dash application
- `app.yaml` - Databricks app configuration
- `requirements.txt` - Python dependencies

## Deployment
The app is configured to deploy to Databricks using the DEFAULT profile.
